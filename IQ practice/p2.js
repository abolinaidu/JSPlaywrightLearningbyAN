// Table Print using JavaScript

let num = parseInt(prompt("Enter a number to print its table:"));

console.log("Table of " + num + ":");  
for (let i=1;i<=10;i++) {
    {
        console.log(num + "*" +i +"=" + num*i)
    }
}